function useDataTypeInLambda() -> Boolean { 
  let object = (True, False); 
  let andPair = \() -> Boolean {
    let (l1, r1) = object; 
    if l1 then r1 else False
  }; 
  andPair()
}

test useDataTypeInLambda = 
  { useDataTypeInLambda() }
