export memory 1000 as memory

import wasi_snapshot_preview1.fd_write as fdWrite(
  filenumber: Int32, 
  startingpoint: (Int32,Int32), 
  c: Int32, 
  d: Int32
) -> Int32

export function _start() -> Int32 { 
  let array = ([ 104, 111, 114, 115, 101 ]: Array(5, Int8)); 
    let iovec = (start(array),size(array));
    fdWrite(1,iovec,1,28)
}

import wasi_snapshot_preview1.fd_write as fdWriteOld(
  filenumber: Int32, 
  startingpoint: Int32,
  c: Int32, 
  d: Int32
) -> Int32


function old() -> Int32 {
  store(4, (5: Int32)); 
  store(0, (8: Int32)); 
  store(8, (104: Int8)); 
  store(9, (111: Int8)); 
  store(10, (114: Int8)); 
  store(11, (115: Int8)); 
  store(12, (101: Int8)); 
  fdWriteOld(1, 0, 1, 28)

}
